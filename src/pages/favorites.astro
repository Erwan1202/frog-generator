---
import clientPromise from "../lib/mongodb";
import { ObjectId } from "mongodb";
import "../styles/global.css";

const userIdCookie = Astro.cookies.get("user_id");
if (!userIdCookie) return Astro.redirect("/login");

const client = await clientPromise;
const db = client.db("frog_app");
const users = db.collection("users");

const user = await users.findOne({ _id: new ObjectId(userIdCookie.value) });
if (!user) return Astro.redirect("/login");

if (Astro.request.method === "POST") {
    const data = await Astro.request.formData();
    const idToDelete = data.get("id_to_delete");
    if (idToDelete) {
        await users.updateOne({ _id: user._id }, { $pull: { favorites: { id: idToDelete } } });
        return Astro.redirect("/favorites");
    }
}

const favorites = (user.favorites || []).reverse();
---

<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Ma Collection - Froggy</title>
    <meta name="description" content="Ta collection personnelle de grenouilles préférées">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Nunito', sans-serif; }
        
        .grid-item {
            opacity: 0;
            animation: grid-appear 0.5s ease forwards;
        }
        
        @keyframes grid-appear {
            from { opacity: 0; transform: scale(0.8) translateY(20px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }
        
        .img-zoom { transition: transform 0.7s cubic-bezier(0.4, 0, 0.2, 1); }
        .img-zoom:hover { transform: scale(1.1); }
        
        @keyframes btn-shine {
            0% { left: -100%; }
            100% { left: 100%; }
        }
        
        .btn-shine::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 50%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
        }
        
        .btn-shine:hover::after { animation: btn-shine 0.6s ease; }
    </style>
</head>
<body class="bg-slate-950 min-h-screen text-white">

    <!-- Animated Aquatic Background -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div class="absolute inset-0 bg-gradient-to-b from-slate-950 via-cyan-950/20 to-blue-950/30"></div>
        
        <div class="absolute top-[-10%] right-[-5%] w-[400px] h-[400px] bg-cyan-500/15 rounded-full blur-[120px] animate-blob"></div>
        <div class="absolute bottom-[-10%] left-[-10%] w-[500px] h-[500px] bg-blue-500/10 rounded-full blur-[140px] animate-blob" style="animation-delay: -5s;"></div>
        <div class="absolute top-1/3 left-1/3 w-[300px] h-[300px] bg-sky-500/10 rounded-full blur-[100px] animate-blob" style="animation-delay: -8s;"></div>
        
        <!-- Bubbles -->
        <div class="absolute bottom-0 left-[20%] w-3 h-3 bg-cyan-400/20 rounded-full animate-bubble" style="animation-delay: 1s;"></div>
        <div class="absolute bottom-0 right-[35%] w-4 h-4 bg-blue-400/15 rounded-full animate-bubble" style="animation-delay: 3s;"></div>
    </div>

    <!-- Subtle Pattern -->
    <div class="fixed inset-0 opacity-[0.015] pointer-events-none" 
         style="background-image: radial-gradient(circle at 1px 1px, white 1px, transparent 0); background-size: 50px 50px;">
    </div>

    <!-- Header Navigation -->
    <nav class="sticky top-0 z-40 bg-slate-950/70 backdrop-blur-xl border-b border-cyan-500/10">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 py-4 flex justify-between items-center">
            <div class="animate-slide-up">
                <h1 class="text-2xl sm:text-3xl font-black bg-gradient-to-r from-cyan-300 via-blue-300 to-sky-300 bg-clip-text text-transparent">
                    Collection
                </h1>
                <div class="flex items-center gap-2 mt-1">
                    <span class="w-2 h-2 bg-cyan-400 rounded-full animate-pulse"></span>
                    <p class="text-xs text-cyan-300/50">
                        {favorites.length} grenouille{favorites.length > 1 ? 's' : ''} sauvée{favorites.length > 1 ? 's' : ''}
                    </p>
                </div>
            </div>
            <div class="flex items-center gap-3 animate-slide-up" style="animation-delay: 0.1s;">
                <a href="/logout" class="px-4 py-2 text-xs font-bold text-red-400 hover:text-white bg-red-500/10 hover:bg-red-500/30 border border-red-500/20 hover:border-red-500/50 rounded-full transition-all duration-300">
                    Déconnexion
                </a>
                <div class="relative group">
                    <div class="absolute inset-0 bg-gradient-to-br from-cyan-400 to-blue-600 rounded-full blur opacity-50 group-hover:opacity-75 transition-opacity"></div>
                    <div class="relative w-10 h-10 bg-gradient-to-br from-cyan-400 to-blue-600 rounded-full flex items-center justify-center text-white font-bold shadow-lg border-2 border-white/20">
                        {user.username.charAt(0).toUpperCase()}
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <main class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 py-6 pb-32">
        
        {favorites.length === 0 ? (
            <!-- Empty State -->
            <div class="flex flex-col items-center justify-center py-24 text-center animate-slide-up">
                <div class="relative mb-8">
                    <div class="absolute inset-0 bg-cyan-500/20 rounded-full blur-3xl scale-150"></div>
                    <div class="relative text-8xl animate-float">🐸</div>
                </div>
                
                <div class="space-y-3 max-w-sm">
                    <h2 class="text-2xl font-black text-white">C'est bien vide ici...</h2>
                    <p class="text-cyan-300/50 text-sm leading-relaxed">
                        Ta collection attend ses premières pensionnaires ! Retourne à l'accueil pour découvrir des grenouilles uniques.
                    </p>
                </div>
                
                <a href="/" 
                   class="mt-8 px-8 py-4 bg-gradient-to-r from-cyan-500 to-blue-500 hover:from-cyan-400 hover:to-blue-400 
                          text-white rounded-full font-bold shadow-xl shadow-cyan-500/30 
                          transition-all duration-300 transform hover:scale-105 hover:-translate-y-1
                          flex items-center gap-3 glow-cyan-hover">
                    <span class="text-xl">🔍</span>
                    Partir à la chasse
                </a>
                
                <div class="flex gap-4 mt-12 opacity-30">
                    <div class="text-3xl animate-float" style="animation-delay: -1s;">🌊</div>
                    <div class="text-2xl animate-float" style="animation-delay: -2s;">💧</div>
                    <div class="text-3xl animate-float" style="animation-delay: -3s;">🐸</div>
                </div>
            </div>
        ) : (
            <Fragment>
                <!-- Stats Bar -->
                <div class="mb-6 p-4 rounded-2xl glass border border-cyan-500/10 flex items-center justify-between animate-slide-up">
                    <div class="flex items-center gap-4">
                        <div class="w-12 h-12 rounded-xl bg-gradient-to-br from-cyan-500/20 to-blue-500/20 flex items-center justify-center border border-cyan-500/20">
                            <span class="text-2xl">📊</span>
                        </div>
                        <div>
                            <p class="text-xs text-cyan-300/50">Ta collection</p>
                            <p class="text-lg font-bold text-white">{favorites.length} grenouille{favorites.length > 1 ? 's' : ''}</p>
                        </div>
                    </div>
                    <a href="/" class="px-4 py-2 text-sm font-bold text-cyan-400 hover:text-white bg-cyan-500/10 hover:bg-cyan-500/20 border border-cyan-500/20 hover:border-cyan-500/40 rounded-full transition-all">
                        + En ajouter
                    </a>
                </div>

                <!-- Grid -->
                <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-3 sm:gap-4">
                    {favorites.map((frog: any, index: number) => (
                        <div 
                            class="grid-item relative group rounded-2xl overflow-hidden aspect-[3/4] bg-slate-900 border border-cyan-500/10 shadow-lg hover:shadow-2xl hover:shadow-cyan-500/10 transition-shadow duration-500"
                            style={`animation-delay: ${index * 0.05}s;`}
                        >
                            <img 
                                src={frog.url} 
                                alt="Grenouille" 
                                class="w-full h-full object-cover img-zoom"
                                loading="lazy"
                                decoding="async"
                            />
                            
                            <div class="absolute inset-0 bg-gradient-to-t from-slate-950/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                            
                            <div class="absolute inset-0 flex items-center justify-center gap-3 opacity-0 group-hover:opacity-100 transition-all duration-300 bg-slate-950/30 backdrop-blur-[2px]">
                                <form method="POST">
                                    <input type="hidden" name="id_to_delete" value={frog.id} />
                                    <button 
                                        type="submit" 
                                        class="relative w-14 h-14 flex items-center justify-center bg-red-500/90 hover:bg-red-500 text-white rounded-full shadow-xl 
                                               hover:scale-110 transition-all duration-300 border-2 border-red-400/30 overflow-hidden btn-shine"
                                        title="Supprimer"
                                    >
                                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-6 h-6">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M14.74 9l-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 01-2.244 2.077H8.084a2.25 2.25 0 01-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 00-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 013.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 00-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 00-7.5 0" />
                                        </svg>
                                    </button>
                                </form>
                            </div>

                            <div class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                                <div class="px-2 py-1 rounded-full bg-slate-950/60 backdrop-blur-sm border border-cyan-500/20">
                                    <span class="text-[9px] font-bold text-cyan-300/70">#{frog.id.slice(0,4)}</span>
                                </div>
                            </div>

                            <div class="absolute bottom-2 left-2 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                                <div class="w-8 h-8 rounded-full bg-cyan-500/20 backdrop-blur-sm flex items-center justify-center border border-cyan-500/30">
                                    <span class="text-sm">💙</span>
                                </div>
                            </div>
                        </div>
                    ))}
                </div>
            </Fragment>
        )}
        
        <div class="h-20"></div>
    </main>

    <!-- Bottom Navigation -->
    <nav class="fixed bottom-0 left-0 w-full bg-slate-950/80 backdrop-blur-xl border-t border-cyan-500/10 flex justify-around py-4 z-50">
        <a href="/" class="flex flex-col items-center gap-1 text-slate-500 hover:text-white transition-colors group">
            <span class="text-xl group-hover:-translate-y-1 transition-transform">🏠</span>
            <span class="text-[10px] font-bold">Accueil</span>
        </a>
        <a href="/favorites" class="flex flex-col items-center gap-1 text-cyan-400 group">
            <div class="relative">
                <span class="absolute -inset-2 bg-cyan-500/20 rounded-full blur-md"></span>
                <span class="relative text-xl">❤️</span>
            </div>
            <span class="text-[10px] font-bold">Favoris</span>
        </a>
        <a href="/social" class="flex flex-col items-center gap-1 text-slate-500 hover:text-white transition-colors group">
            <span class="text-xl group-hover:-translate-y-1 transition-transform">👥</span>
            <span class="text-[10px] font-bold">Social</span>
        </a>
        <a href="/profile" class="flex flex-col items-center gap-1 text-slate-500 hover:text-white transition-colors group">
            <span class="text-xl group-hover:-translate-y-1 transition-transform">👤</span>
            <span class="text-[10px] font-bold">Profil</span>
        </a>
    </nav>

</body>
</html>