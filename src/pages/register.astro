---
import clientPromise from "../lib/mongodb";
import bcrypt from "bcryptjs";
import "../styles/global.css";

let message = "";
let error = false;

if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();
    const username = data.get("username")?.toString();
    const password = data.get("password")?.toString();

    if (!username || !password) {
      message = "Tous les champs sont requis.";
      error = true;
    } else {
      const client = await clientPromise;
      const db = client.db("frog_app");
      const users = db.collection("users");

      const existingUser = await users.findOne({ username });
      if (existingUser) {
        message = "Ce pseudo est déjà pris par une autre grenouille !";
        error = true;
      } else {
        const hashedPassword = await bcrypt.hash(password.toString(), 10);

        await users.insertOne({
          username,
          password: hashedPassword,
          favorites: [],
          createdAt: new Date(),
        });

        return Astro.redirect("/login");
      }
    }
  } catch (e) {
    console.error(e);
    message = "Une erreur serveur est survenue.";
    error = true;
  }
}
---

<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Inscription - Froggy</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Nunito", sans-serif;
      }
    </style>
  </head>
  <body
    class="bg-slate-950 min-h-screen flex items-center justify-center overflow-hidden relative"
  >
    <!-- Animated Aquatic Background -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
      <div
        class="absolute inset-0 bg-gradient-to-b from-slate-950 via-blue-950/30 to-cyan-950/40"
      >
      </div>

      <div
        class="absolute top-[-20%] right-[-10%] w-[500px] h-[500px] bg-blue-500/20 rounded-full blur-[120px] animate-blob"
      >
      </div>
      <div
        class="absolute bottom-[-20%] left-[-10%] w-[600px] h-[600px] bg-cyan-500/15 rounded-full blur-[150px] animate-blob"
        style="animation-delay: -3s;"
      >
      </div>
      <div
        class="absolute top-1/3 left-1/4 w-[300px] h-[300px] bg-sky-400/10 rounded-full blur-[80px] animate-blob"
        style="animation-delay: -7s;"
      >
      </div>

      <!-- Bubbles -->
      <div
        class="absolute bottom-0 left-[20%] w-4 h-4 bg-cyan-400/30 rounded-full animate-bubble"
        style="animation-delay: 1s;"
      >
      </div>
      <div
        class="absolute bottom-0 left-[40%] w-3 h-3 bg-blue-400/20 rounded-full animate-bubble"
        style="animation-delay: 3s;"
      >
      </div>
      <div
        class="absolute bottom-0 right-[30%] w-5 h-5 bg-sky-400/25 rounded-full animate-bubble"
        style="animation-delay: 0s;"
      >
      </div>
    </div>

    <!-- Floating Decorations -->
    <div
      class="fixed top-16 left-10 text-5xl opacity-20 animate-float hidden md:block"
      style="animation-delay: -1s;"
    >
      🐸
    </div>
    <div
      class="fixed bottom-32 right-16 text-3xl opacity-15 animate-wave hidden md:block"
      style="animation-delay: -3s;"
    >
      🌊
    </div>
    <div
      class="fixed top-1/3 right-20 text-2xl opacity-10 animate-float hidden lg:block"
      style="animation-delay: -5s;"
    >
      ✨
    </div>

    <!-- Main Card -->
    <div class="relative z-10 w-full max-w-md mx-4">
      <div
        class="glass rounded-3xl p-8 md:p-10 shadow-2xl animate-slide-up border border-cyan-500/10"
      >
        <!-- Logo & Title -->
        <div class="text-center mb-8">
          <div
            class="inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-gradient-to-br from-blue-400 to-cyan-600 mb-6 shadow-lg glow-cyan animate-float"
          >
            <span class="text-4xl">🌊</span>
          </div>
          <h1 class="text-3xl md:text-4xl font-black text-white mb-2">
            Rejoins la mare
          </h1>
          <p class="text-cyan-200/60 text-sm">
            Crée ton compte et plonge dans l'aventure
          </p>
        </div>

        <!-- Message -->
        {
          message && (
            <div
              class={`mb-6 p-4 rounded-xl backdrop-blur-sm animate-slide-up ${error ? "bg-red-500/20 border border-red-500/30" : "bg-cyan-500/20 border border-cyan-500/30"}`}
            >
              <div class="flex items-center gap-3">
                <span class="text-xl">{error ? "⚠️" : "✅"}</span>
                <span
                  class={`font-semibold text-sm ${error ? "text-red-300" : "text-cyan-300"}`}
                >
                  {message}
                </span>
              </div>
            </div>
          )
        }

        <!-- Form -->
        <form method="POST" class="space-y-5">
          <div
            class="space-y-2 animate-slide-up stagger-1 opacity-0"
            style="animation-delay: 0.1s;"
          >
            <label class="block text-cyan-100/80 text-sm font-bold ml-1"
              >Pseudo</label
            >
            <div class="relative group">
              <div
                class="absolute inset-0 bg-gradient-to-r from-cyan-500/50 to-blue-500/50 rounded-xl blur opacity-0 group-focus-within:opacity-100 transition-opacity"
              >
              </div>
              <input
                type="text"
                name="username"
                minlength="3"
                maxlength="20"
                class="relative w-full px-5 py-4 bg-white/5 border border-cyan-500/20 rounded-xl text-white placeholder-cyan-200/30 focus:outline-none focus:border-cyan-400/50 focus:bg-white/10 transition-all"
                placeholder="Choisis un pseudo unique..."
                required
              />
            </div>
            <p class="text-cyan-300/40 text-xs ml-1">3-20 caractères</p>
          </div>

          <div
            class="space-y-2 animate-slide-up stagger-2 opacity-0"
            style="animation-delay: 0.2s;"
          >
            <label class="block text-cyan-100/80 text-sm font-bold ml-1"
              >Mot de passe</label
            >
            <div class="relative group">
              <div
                class="absolute inset-0 bg-gradient-to-r from-cyan-500/50 to-blue-500/50 rounded-xl blur opacity-0 group-focus-within:opacity-100 transition-opacity"
              >
              </div>
              <input
                type="password"
                name="password"
                minlength="6"
                class="relative w-full px-5 py-4 bg-white/5 border border-cyan-500/20 rounded-xl text-white placeholder-cyan-200/30 focus:outline-none focus:border-cyan-400/50 focus:bg-white/10 transition-all"
                placeholder="Minimum 6 caractères..."
                required
              />
            </div>
            <p class="text-cyan-300/40 text-xs ml-1">Minimum 6 caractères</p>
          </div>

          <button
            type="submit"
            class="w-full mt-6 py-4 bg-gradient-to-r from-blue-500 to-cyan-500 hover:from-blue-400 hover:to-cyan-400 text-white font-bold rounded-xl transition-all transform hover:scale-[1.02] hover:shadow-lg hover:shadow-cyan-500/25 active:scale-[0.98] animate-slide-up stagger-3 opacity-0 glow-cyan-hover"
            style="animation-delay: 0.3s;"
          >
            <span class="flex items-center justify-center gap-2">
              🐸 Créer mon compte
            </span>
          </button>
        </form>

        <!-- Features -->
        <div
          class="mt-8 p-4 rounded-xl bg-cyan-500/5 border border-cyan-500/10"
        >
          <p class="text-cyan-300/50 text-xs text-center mb-3 font-semibold">
            Ce qui t'attend :
          </p>
          <div class="flex justify-around text-center">
            <div class="flex flex-col items-center gap-1">
              <span class="text-2xl">🐸</span>
              <span class="text-cyan-300/40 text-[10px]">Grenouilles</span>
            </div>
            <div class="flex flex-col items-center gap-1">
              <span class="text-2xl">💙</span>
              <span class="text-cyan-300/40 text-[10px]">Favoris</span>
            </div>
            <div class="flex flex-col items-center gap-1">
              <span class="text-2xl">✨</span>
              <span class="text-cyan-300/40 text-[10px]">Collection</span>
            </div>
          </div>
        </div>

        <!-- Divider -->
        <div class="flex items-center gap-4 my-8">
          <div
            class="flex-1 h-px bg-gradient-to-r from-transparent via-cyan-500/30 to-transparent"
          >
          </div>
          <span class="text-cyan-300/40 text-xs font-semibold">ou</span>
          <div
            class="flex-1 h-px bg-gradient-to-r from-transparent via-cyan-500/30 to-transparent"
          >
          </div>
        </div>

        <!-- Login Link -->
        <p
          class="text-center text-cyan-200/50 text-sm animate-slide-up stagger-4 opacity-0"
          style="animation-delay: 0.4s;"
        >
          Déjà une grenouille ?
          <a
            href="/login"
            class="text-cyan-400 hover:text-cyan-300 font-bold transition-colors hover:underline decoration-2 underline-offset-2"
          >
            Se connecter 🌊
          </a>
        </p>
      </div>

      <!-- Footer -->
      <p
        class="text-center text-cyan-900 text-xs mt-6 animate-fade-in"
        style="animation-delay: 0.6s;"
      >
        Froggy Love © 2024 • Fait avec 💙
      </p>
    </div>
  </body>
</html>
