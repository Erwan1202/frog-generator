---
import clientPromise from "../lib/mongodb";
import bcrypt from "bcryptjs";
import "../styles/global.css";

let message = "";

if (Astro.request.method === "POST") {
  const data = await Astro.request.formData();
  const username = data.get("username");
  const password = data.get("password");

  if (username && password) {
    const client = await clientPromise;
    const db = client.db("frog_app");
    const users = db.collection("users");

    const user = await users.findOne({ username: username.toString() });

    if (user) {
      const validPass = await bcrypt.compare(
        password.toString(),
        user.password,
      );

      if (validPass) {
        Astro.cookies.set("user_id", user._id.toString(), {
          path: "/",
          httpOnly: true,
          maxAge: 60 * 60 * 24 * 7,
        });

        return Astro.redirect("/");
      } else {
        message = "Mot de passe incorrect.";
      }
    } else {
      message = "Utilisateur inconnu.";
    }
  } else {
    message = "Tous les champs sont requis.";
  }
}
---

<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Connexion - Froggy</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Nunito", sans-serif;
      }
    </style>
  </head>
  <body
    class="bg-slate-950 min-h-screen flex items-center justify-center overflow-hidden relative"
  >
    <!-- Animated Aquatic Background -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
      <!-- Water gradient overlay -->
      <div
        class="absolute inset-0 bg-gradient-to-b from-slate-950 via-cyan-950/30 to-blue-950/50"
      >
      </div>

      <!-- Animated blobs -->
      <div
        class="absolute top-[-20%] left-[-10%] w-[500px] h-[500px] bg-cyan-500/20 rounded-full blur-[120px] animate-blob"
      >
      </div>
      <div
        class="absolute bottom-[-20%] right-[-10%] w-[600px] h-[600px] bg-blue-500/15 rounded-full blur-[150px] animate-blob"
        style="animation-delay: -3s;"
      >
      </div>
      <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[400px] h-[400px] bg-sky-500/10 rounded-full blur-[100px] animate-blob"
        style="animation-delay: -5s;"
      >
      </div>

      <!-- Bubbles -->
      <div
        class="absolute bottom-0 left-[10%] w-4 h-4 bg-cyan-400/30 rounded-full animate-bubble"
        style="animation-delay: 0s;"
      >
      </div>
      <div
        class="absolute bottom-0 left-[25%] w-3 h-3 bg-sky-400/20 rounded-full animate-bubble"
        style="animation-delay: 2s;"
      >
      </div>
      <div
        class="absolute bottom-0 left-[60%] w-5 h-5 bg-blue-400/25 rounded-full animate-bubble"
        style="animation-delay: 4s;"
      >
      </div>
      <div
        class="absolute bottom-0 right-[20%] w-3 h-3 bg-cyan-300/20 rounded-full animate-bubble"
        style="animation-delay: 1s;"
      >
      </div>
    </div>

    <!-- Floating Decoration -->
    <div
      class="fixed top-10 right-10 text-6xl opacity-20 animate-float hidden md:block"
    >
      🐸
    </div>
    <div
      class="fixed bottom-20 left-10 text-4xl opacity-15 animate-wave hidden md:block"
      style="animation-delay: -2s;"
    >
      🌊
    </div>
    <div
      class="fixed top-1/4 left-[15%] text-3xl opacity-10 animate-float hidden lg:block"
      style="animation-delay: -3s;"
    >
      💧
    </div>

    <!-- Main Card -->
    <div class="relative z-10 w-full max-w-md mx-4">
      <div
        class="glass rounded-3xl p-8 md:p-10 shadow-2xl animate-slide-up border border-cyan-500/10"
      >
        <!-- Logo & Title -->
        <div class="text-center mb-8">
          <div
            class="inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-gradient-to-br from-cyan-400 to-blue-600 mb-6 shadow-lg glow-cyan animate-pulse-glow"
          >
            <span class="text-4xl">🐸</span>
          </div>
          <h1 class="text-3xl md:text-4xl font-black text-white mb-2">
            Bon retour !
          </h1>
          <p class="text-cyan-200/60 text-sm">
            Plonge dans la mare aux grenouilles
          </p>
        </div>

        <!-- Error Message -->
        {
          message && (
            <div class="mb-6 p-4 rounded-xl bg-red-500/20 border border-red-500/30 backdrop-blur-sm animate-slide-up">
              <div class="flex items-center gap-3">
                <span class="text-xl">⚠️</span>
                <span class="text-red-300 font-semibold text-sm">
                  {message}
                </span>
              </div>
            </div>
          )
        }

        <!-- Form -->
        <form method="POST" class="space-y-5">
          <div
            class="space-y-2 animate-slide-up stagger-1 opacity-0"
            style="animation-delay: 0.1s;"
          >
            <label class="block text-cyan-100/80 text-sm font-bold ml-1"
              >Pseudo</label
            >
            <div class="relative group">
              <div
                class="absolute inset-0 bg-gradient-to-r from-cyan-500/50 to-blue-500/50 rounded-xl blur opacity-0 group-focus-within:opacity-100 transition-opacity"
              >
              </div>
              <input
                type="text"
                name="username"
                class="relative w-full px-5 py-4 bg-white/5 border border-cyan-500/20 rounded-xl text-white placeholder-cyan-200/30 focus:outline-none focus:border-cyan-400/50 focus:bg-white/10 transition-all"
                placeholder="Ton pseudo de grenouille..."
                required
              />
            </div>
          </div>

          <div
            class="space-y-2 animate-slide-up stagger-2 opacity-0"
            style="animation-delay: 0.2s;"
          >
            <label class="block text-cyan-100/80 text-sm font-bold ml-1"
              >Mot de passe</label
            >
            <div class="relative group">
              <div
                class="absolute inset-0 bg-gradient-to-r from-cyan-500/50 to-blue-500/50 rounded-xl blur opacity-0 group-focus-within:opacity-100 transition-opacity"
              >
              </div>
              <input
                type="password"
                name="password"
                class="relative w-full px-5 py-4 bg-white/5 border border-cyan-500/20 rounded-xl text-white placeholder-cyan-200/30 focus:outline-none focus:border-cyan-400/50 focus:bg-white/10 transition-all"
                placeholder="••••••••"
                required
              />
            </div>
          </div>

          <button
            type="submit"
            class="w-full mt-6 py-4 bg-gradient-to-r from-cyan-500 to-blue-500 hover:from-cyan-400 hover:to-blue-400 text-white font-bold rounded-xl transition-all transform hover:scale-[1.02] hover:shadow-lg hover:shadow-cyan-500/25 active:scale-[0.98] animate-slide-up stagger-3 opacity-0 glow-cyan-hover"
            style="animation-delay: 0.3s;"
          >
            <span class="flex items-center justify-center gap-2">
              Se connecter
            </span>
          </button>
        </form>

        <!-- Divider -->
        <div class="flex items-center gap-4 my-8">
          <div
            class="flex-1 h-px bg-gradient-to-r from-transparent via-cyan-500/30 to-transparent"
          >
          </div>
          <span class="text-cyan-300/40 text-xs font-semibold">ou</span>
          <div
            class="flex-1 h-px bg-gradient-to-r from-transparent via-cyan-500/30 to-transparent"
          >
          </div>
        </div>

        <!-- Register Link -->
        <p
          class="text-center text-cyan-200/50 text-sm animate-slide-up stagger-4 opacity-0"
          style="animation-delay: 0.4s;"
        >
          Pas encore de compte ?
          <a
            href="/register"
            class="text-cyan-400 hover:text-cyan-300 font-bold transition-colors hover:underline decoration-2 underline-offset-2"
          >
            Plonge avec nous 🐸
          </a>
        </p>
      </div>

      <!-- Footer -->
      <p
        class="text-center text-cyan-900 text-xs mt-6 animate-fade-in"
        style="animation-delay: 0.6s;"
      >
        Froggy Love © 2024 • Fait avec �
      </p>
    </div>
  </body>
</html>
