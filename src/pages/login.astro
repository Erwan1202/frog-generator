---
import clientPromise from "../lib/mongodb";
import bcrypt from "bcryptjs";

let message = "";

if (Astro.request.method === "POST") {
  const data = await Astro.request.formData();
  const username = data.get("username");
  const password = data.get("password");

  if (username && password) {
    const client = await clientPromise;
    const db = client.db("frog_app");
    const users = db.collection("users");

    const user = await users.findOne({ username: username.toString() });

    if (user) {
      const validPass = await bcrypt.compare(password.toString(), user.password);
      
      if (validPass) {
        Astro.cookies.set("user_id", user._id.toString(), {
          path: "/",
          httpOnly: true,
          maxAge: 60 * 60 * 24 * 7
        });
        
        return Astro.redirect("/");
      } else {
        message = "Mot de passe incorrect.";
      }
    } else {
      message = "Utilisateur inconnu.";
    }
  } else {
    message = "Tous les champs sont requis.";
  }
}
---

<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion - Froggy</title>
</head>
<body class="bg-green-50 min-h-screen flex items-center justify-center font-sans">

    <div class="bg-white p-8 rounded-2xl shadow-xl w-full max-w-md border-t-4 border-green-500">
        <h1 class="text-3xl font-bold text-green-700 mb-6 text-center">Retour à la mare 🐸</h1>

        {message && (
            <div class="p-3 mb-4 rounded bg-red-100 text-red-700">
                {message}
            </div>
        )}

        <form method="POST" class="flex flex-col gap-4">
            <div>
                <label class="block text-gray-700 text-sm font-bold mb-2">Pseudo</label>
                <input type="text" name="username" class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-500" required />
            </div>
            
            <div>
                <label class="block text-gray-700 text-sm font-bold mb-2">Mot de passe</label>
                <input type="password" name="password" class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-500" required />
            </div>

            <button type="submit" class="bg-green-600 hover:bg-green-700 text-white font-bold py-3 rounded transition-colors mt-2">
                Se connecter
            </button>
        </form>

        <p class="mt-4 text-center text-sm text-gray-600">
            Pas encore de compte ? <a href="/register" class="text-green-600 hover:underline">S'inscrire</a>
        </p>
    </div>

</body>
</html>